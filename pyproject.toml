[project]
name = "ticket-triage"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Pradhnyesh Khorgade", email = "pradhnyeshk@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mlflow>=2.15,<3",
    "openai>=2.53.0",
    "pandas>=2.2,<3",
    "psycopg[binary]>=3.3.4",
    "python-dotenv>=1.2.2",
    "scikit-learn>=1.9.0",
    "xgboost>=3.4.0",
]

[project.scripts]
ticket-triage = "ticket_triage:main"

[build-system]
requires = ["uv_build>=0.11.29,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
    "matplotlib>=3.11.1",
    "nbconvert>=7.17.1",
    "pytest>=9.1.1",
]
